name: Guard FlameCore

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Refuse tracked FlameCore source
        run: node scripts/assert-no-tracked-flamecore.mjs
