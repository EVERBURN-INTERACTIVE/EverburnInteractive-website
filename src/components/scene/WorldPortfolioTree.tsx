'use client';

import { TreeCluster } from './TreeCluster';

/** Same animated tree cluster used on the homepage grid corners. */
export function WorldPortfolioTree() {
  return <TreeCluster x={1} z={1} tileIcon />;
}
