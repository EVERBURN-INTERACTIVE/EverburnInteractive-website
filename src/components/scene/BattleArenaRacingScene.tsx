'use client';

/**
 * Previous 3D racing shell. One More Second now boots as its own attract
 * overlay from SceneCanvas; this module is kept so old dynamic imports
 * do not break during HMR.
 */
export function BattleArenaRacingScene(_props: { active: boolean }) {
  return null;
}
